# PDF-Vectorisation-Pipeline
PDF vectorization pipeline using LangChain + LangGraph — ingests PDFs into a Chroma vector store and exposes a raw similarity-search REST API. No LLM chains on retrieval.
